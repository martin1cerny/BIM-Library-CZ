<UserControl x:Class="BLSpec.Controls.ClassificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="self">
    <Grid>
        <TreeView ItemsSource="{Binding ElementName=self, Path=Classification.RootItems}" 
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  MouseDown="TreeView_MouseDown"
                  Name="twClassification">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                    <TextBlock Text="{Binding LocalizedName}" MouseDown="TreeView_MouseDown"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
